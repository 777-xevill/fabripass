import Demo from './prototype';
export const metadata={title:'Live garment scan & track demo | FabriPass',description:'Generate sample garment QR codes, scan from another phone and watch saved checkpoint events appear in a shared tracking history.'};
export default function Page(){return <Demo/>}
